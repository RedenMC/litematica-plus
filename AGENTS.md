# Code Style

The legacy code in fi.dy.masa uses == false for boolean checks, and add a newline before the opening brace of if/else...

This should be kept.

However, in newer code in me.zly2006.rvc, we can use more modern Java style, such as using ! for boolean checks, and keeping the opening brace on the same line.
